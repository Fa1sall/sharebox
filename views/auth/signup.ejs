<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ShareBox | Sign Up</title>
    <link rel="stylesheet" href="/styles/output.css" />
  </head>
  <body class="min-h-screen flex items-center justify-center bg-gray-100 px-4">
    <div class="bg-white py-8 px-6 rounded shadow-md w-full max-w-md">
      <h2 class="text-2xl font-bold text-center mb-6">Sign Up</h2>
      <form action="/signup" method="post" class="flex flex-col gap-4">
        <div>
          <label for="email" class="block mb-1">Email</label>
          <input type="email" name="email" id="email" required
            value="<%= formData?.email || '' %>"
            placeholder="Enter your email"
            class="w-full border rounded px-3 py-2 focus:outline-none focus:ring"
          />
          <% if(errors && errors["email"]) { %>
            <p class="text-red-500 text-sm mt-1"><%= errors["email"].msg %></p>
          <% } %>
        </div>

        <div>
          <label for="password" class="block mb-1">Password</label>
          <input type="password" name="password" id="password" required
            placeholder="Enter your password"
            class="w-full border rounded px-3 py-2 focus:outline-none focus:ring"
          />
          <% if(errors && errors["password"]) { %>
            <p class="text-red-500 text-sm mt-1"><%= errors["password"].msg %></p>
          <% } %>
        </div>

        <div>
          <label for="confirm_password" class="block mb-1">Confirm Password</label>
          <input type="password" name="confirm_password" id="confirm_password" required
            placeholder="Re-enter your password"
            class="w-full border rounded px-3 py-2 focus:outline-none focus:ring"
          />
          <% if(errors && errors["confirm_password"]) { %>
            <p class="text-red-500 text-sm mt-1"><%= errors["confirm_password"].msg %></p>
          <% } %>
        </div>

        <% if(errors && errors["signup_error"]) { %>
          <p class="text-red-500 text-sm mt-2 text-center"><%= errors["signup_error"] %></p>
        <% } %>

        <button type="submit" class="mt-4 w-full bg-green-600 text-white rounded py-2 hover:bg-green-700 transition">
          Sign Up
        </button>
      </form>

      <p class="mt-4 text-center text-sm">
        Already have an account? <a href="/login" class="text-blue-600 hover:underline">Login</a>
      </p>
    </div>
  </body>
</html>
